# Everything Zane needs to run except the retrieval-augmented-memory
# stack (see requirements-memory.txt) — kept separate because
# sentence-transformers pulls in torch, which is by far the heaviest and
# slowest part of the build. Without it, PersistentMemory degrades
# gracefully: durable SQLite history and the rolling in-context window
# still work fully, only semantic retrieval/embedding are skipped (see
# zane/memory/embeddings.py, zane/memory/vector_index.py).

# Core
groq>=0.11.0
python-dotenv>=1.0.1
pydantic>=2.7.0

# Web search pipeline ("infinite information")
ddgs>=9.0.0
tavily-python>=0.5.0  # optional: used automatically if TAVILY_API_KEY is set

# Voice (ElevenLabs text-to-speech), optional: sessions run text-only
# without this installed or without ELEVENLABS_API_KEY/ZANE_VOICE_ID set.
elevenlabs>=1.9.0
httpx>=0.27.0

# API deployment surface
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
# Required by FastAPI/Starlette to parse multipart form uploads
# (/voice/enroll and /voice/verify take an audio file). Needed
# unconditionally, independent of the optional speaker-verification
# extra in requirements-voice-verify.txt.
python-multipart>=0.0.9

# C++ extension build
pybind11>=2.12.0
