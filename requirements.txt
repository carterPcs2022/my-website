# Full local-dev install: everything Zane can use, plus test tooling.
# For a leaner production build (e.g. a resource-constrained Render build),
# see requirements-core.txt / requirements-memory.txt and Dockerfile.
-r requirements-core.txt
-r requirements-memory.txt
-r requirements-voice-verify.txt

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
