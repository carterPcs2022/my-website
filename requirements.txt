# Render/runtime install: keep the production image lean.
# Optional local semantic-memory dependencies live in requirements-memory.txt.
-r requirements-core.txt

# Tests are harmless for local development; Render does not run them at startup.
pytest>=8.0.0
pytest-asyncio>=0.23.0
